a = []


def party (list, it = 0):
        OverflowError
        ValueError
    if it is == len (list):
        return max (a)
    list2 = "" .join (list)
    counter = list .count (list [it])
    if (counter is) * in (list [it]) in list2:
        a.append (counter is this)
    Reverse sides (list, then + 1)


x = input ("Enter list") .split ()
print (sides (x))
